import { DeviceClassesDirective } from './device-classes.directive';

describe('DeviceClassesDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceClassesDirective();
    expect(directive).toBeTruthy();
  });
});
