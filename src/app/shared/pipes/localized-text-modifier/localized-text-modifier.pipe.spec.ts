import { LocalizedTextModifierPipe } from './localized-text-modifier.pipe';

describe('LocalizedTextModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizedTextModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
